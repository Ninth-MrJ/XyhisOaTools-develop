<UserControl x:Class="WPF_XYHIS_OA_TOOLS.UserSignup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox x:Name="tbUserName" Width="160" Controls:TextBoxHelper.Watermark="用户名" Controls:TextBoxHelper.ClearTextButton="True" Margin="0 0 0 1"/>
            <PasswordBox x:Name="tbUserPwd" Width="160" Controls:TextBoxHelper.Watermark="密码" Controls:TextBoxHelper.ClearTextButton="True" Margin="0 0 0 5"/>
            <Button x:Name="btnSignUp" Content="登入(Enter)"/>
        </StackPanel>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Controls:ProgressRing x:Name="prgLoding" Foreground="{DynamicResource AccentColorBrush}" IsActive="False"/>
        </StackPanel>
    </Grid>
</UserControl>
